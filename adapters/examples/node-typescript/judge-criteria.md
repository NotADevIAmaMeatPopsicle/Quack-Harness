# Project judge criteria

- Every success criterion is supported by code or test evidence.
- Required build, test, and lint commands pass.
- The implementation stays within the task's declared scope.
- No credentials, private paths, or generated runtime state are added.
- User-facing behavior and configuration changes are documented.
