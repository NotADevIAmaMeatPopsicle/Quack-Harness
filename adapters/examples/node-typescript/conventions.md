# Project conventions

- Keep TypeScript strict and avoid untyped boundary values.
- Mirror `src/` structure under `tests/` where practical.
- Add focused tests for behavior changes.
- Keep generated output, credentials, and local machine configuration out of Git.
- Do not change public contracts without updating their documentation and tests.
