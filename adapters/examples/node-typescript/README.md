# Node.js + TypeScript adapter example

Copy this directory to `<PROJECT_PATH>/.quack/`, then adapt every path and command to the target repository before running an agent.

The example is deliberately conservative: it permits edits only to common source, test, and documentation paths; blocks generated and credential-bearing paths; disables automatic push and merge; and requires build, test, and lint checks.
