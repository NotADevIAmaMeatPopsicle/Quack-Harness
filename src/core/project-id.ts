/** Canonical project slug shared by monitor and dispatch authority reads. */
export function generateProjectId(name: string): string {
  return name
    .toLowerCase()
    .trim()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-+|-+$/g, "");
}
